version: "3.8"
volumes:
  my-datavolume:
services:
  mysql-db:
    image: "bitnami/mysql:latest"
    env_file:
      - ./server/.env
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - my-datavolume:/var/lib/mysql
  client:
    build: ./client
    ports:
      - "3307:3306"
    stdin_open: true
    tty: true
    depends_on:
      - server
  server:
    build: ./server
    # build:
    #   context: ./server
    #   dockerfile: Dockerfile
    #   args:
    #     some-arg: 1
    ports:
      - "6868:8080"
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env
    depends_on:
      - mysql-db
